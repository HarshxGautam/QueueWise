# Server Port
PORT=5000

# MongoDB Atlas Connection URI (Optional: System automatically uses In-Memory Engine fallback if omitted or offline)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/queuewise?retryWrites=true&w=majority

# JWT Secret Key for Staff Authentication
JWT_SECRET=queuewise_super_secret_jwt_key_2026

# Google Gemini API Key (Optional: System automatically uses Smart Heuristic engine fallback if omitted)
GEMINI_API_KEY=your_gemini_api_key_here
